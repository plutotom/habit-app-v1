# Convex backend (future sync slice)

Retained for account import and cloud sync. **Not used by the guest-first
offline app.** The live app reads and writes `src/local/` (Expo SQLite) only.

When working on this folder, read `backend/_generated/ai/guidelines.md` first.
Start the dev deployment with `pnpm dev:backend` from the repo root.
